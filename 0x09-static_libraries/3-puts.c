#include "main.h"

/**
 *_puts - Prints the parameter strings to stdout
 *@str: String need to be printed
 *_putchar prints a new line
 *Return: nothing
 */

void _puts(char *s)
{
while (*str)
{
_putchar(*str++);
_putchar('\n');
}
}
