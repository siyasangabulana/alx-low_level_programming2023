#include "main.h"

/**
 * _abs - It computes the absolute value of an integer
 *@n: check the integer
 *Return: The value of an integer
 */

int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
return (-n);
}
